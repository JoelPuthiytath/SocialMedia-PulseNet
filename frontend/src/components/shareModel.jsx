export const ShareModal = () => {
  return (
    <div>
      {/* Add UI elements for selecting friends and confirming sharing */}
      <h2>Share Post</h2>
      <button onClick={closeShareModal}>Close</button>
    </div>
  );
};
